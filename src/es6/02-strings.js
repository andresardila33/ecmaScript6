let hello = 'Hello';
let world = 'World';

// Template literals
const epicPhrase = `${hello} ${world}!!`
console.log(epicPhrase);

// Multi-line strings
let lorem = `Esto es una linea
esto es otra linea
y esto tambien es otra linea`;
console.log(lorem);
